进入 D:\zhineng\tupu 及其子目录进行任何图谱构建、存储、分类、提取、标签、Schema、runtime、星云/三维粒子 OS 投影相关工作前，必须先读取：

D:\zhineng\tupu\ROOT-图谱构建底层逻辑规则.md

读取 ROOT 文件后，必须继续执行 ROOT 中定义的必读顺序和阶段门槛。

未读取该文件前，不得进入 JSON Schema、runtime、真实数据接入、关系状态写入、身份合并、外部动作或学习权重转正。
