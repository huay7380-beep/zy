# Project

这是一个目标导向型人类社交辅助系统项目。第一阶段聚焦 B2B 商务沟通和客户跟进，核心目标是把用户目标、人物关系、事件线索、决策证据、触发计划、状态上报和反馈回写连成可验证闭环�?
# Commands

- 运行冷启动人物引入演示：`npm run identity:cold-start`

- 运行演示：`node scripts/run-demo.mjs`
- 运行社交图谱进程安排：`node scripts/run-social-plan.mjs`
- 运行决策集群演示：`node scripts/run-decision-demo.mjs`
- 运行触发引擎演示：`node scripts/run-trigger-demo.mjs`
- 写出本地自动化预览测试页：`npm run trigger:page`
- 校验平台测试页或页面快照：`npm run platform:snapshot:validate`
- 校验试点样本进入 MVP 前置门禁：`npm run pilot:validate`
- 导入试点样本：`npm run import:pilot`
- 使用导入样本运�??MVP 闭环：`npm run mvp:import`
- 生�??MVP 应用内报告页：`npm run mvp:report`
- 带用户专项测试意见生成二次优化报告：`npm run mvp:feedback-report`
- 运�??MVP 自代理预检：`npm run mvp:self-agent`
- 生�??MVP 外部输入安全模板：`npm run mvp:inputs:init`
- 检??MVP 外部输入就绪状态：`npm run mvp:inputs:check`
- 生�??PT-003 真实试点样本材料交接包：`npm run mvp:pt003:prepare`
- 生�??PT-004 受控平台快照：`npm run mvp:pt004:prepare`
- 运�??MVP 真实输入受控试跑：`npm run mvp:real-trial`
- 生�??MVP 统一状态看板：`npm run mvp:status`
- 生�??MVP 完成度审计产物：`npm run mvp:audit`
- 生�??MVP 总目标逐项审计产物：`npm run mvp:objective:audit`
- 复核只读重�??observation：`npm run intake:read-only:duplicate:review`
- 记录只读重�??observation 操作者确认：`npm run intake:read-only:duplicate:confirm`
- 生成只读批量采集 manifest 交接包：`npm run intake:read-only:manifest:init`
- 校验只读批量采集 manifest 就绪状态：`npm run intake:read-only:manifest:check -- --manifest=runtime/user-inputs/read-only-source-collection.manifest.json`
- 生成统一来源接入矩阵：`npm run intake:source:matrix`
- 生�??Sightflow 目标流程收口报告：`npm run desktop:target-flow:report`
- 运行可能性分支复杂表述演示：`npm run possibility:branch`
- 运行目标导向图谱工程回测：`npm run tupu:backtest`
- 运行工具调用模块演示：`npm run tool:demo`
- 运�??PT-028 恋人关系流程审计和现有记录逐项回放：`npm run pt028:audit`
- 生�??PT-028 GUI 决策状态投影：`npm run pt028:gui-state`
- 生�??PT-028 真�??observation 候�??GUI 状态：`npm run pt028:real-observation-gui-states`
- 生�??PT-028 最终反馈决策包：`npm run pt028:feedback-decision-pack`
- 预�??PT-028 真实反馈确认模板：`npm run pt028:feedback-confirm:preflight`
- 校�??PT-028 最终反馈交接包：`npm run pt028:feedback-handoff:validate`
- 生�??PT-028 逐窗口反馈采集会话：`npm run pt028:feedback-collection:session`
- 校�??PT-028 采集会话与确认模板覆盖：`npm run pt028:feedback-collection:coverage`
- Run PT-028 real feedback finalization: `npm run pt028:feedback-finalize`
- 运�??PT-028 最终专项验收链：`npm run pt028:acceptance-chain`
- 校验流程树�??Obsidian 同步：`npm run process-tree:validate`
- 运�??MVP 闭环：`node scripts/run-mvp-loop.mjs`
- 批量运行三�??MVP 试点闭环并输出门禁指标：`node scripts/run-mvp-loop.mjs --all`
- 运行测试：`node --test packages/agent-runtime/tests/*.test.mjs packages/social-graph/tests/*.test.mjs packages/decision-cluster/tests/*.test.mjs packages/trigger-engine/tests/*.test.mjs packages/storage-runtime/tests/*.test.mjs packages/intake-runtime/tests/*.test.mjs packages/identity-resolution/tests/*.test.mjs packages/possibility-branch/tests/*.test.mjs packages/mvp-runtime/tests/*.test.mjs packages/tool-runtime/tests/*.test.mjs`
- 运行存储运行时测试：`node --test packages/storage-runtime/tests/*.test.mjs`
- 运�??MVP 运行时测试：`node --test packages/mvp-runtime/tests/*.test.mjs`
- 运行工具调用运行时测试：`node --test packages/tool-runtime/tests/*.test.mjs`
- 运行压力测试入口：`node scripts/stress-test.mjs --runs 50`
- 运�??MVP 全链路压力测试：`npm run stress:mvp`

# Style

- 文档优先使用中文，工程文件和标识符使用清晰英文命名�??- 第一版采用“确定性工作�??+ LLM 节点预留”的结构，不直接做全自�??agent??- 人际关系图谱负责对象和关系，事件图谱负责事件和线索，策略层只能读取脱敏摘要和约束??- 决策集群负责综合目标、关系、事件、偏好、技能和反馈证据，输出可解释的推荐选项�?- 推荐选项必须尽量落成具体可人工确认的 `message_draft`，不能只停留在抽象动作名称�?- 新能力先??schema、状态记录和测试样例，再扩展复杂逻辑�?- 文档新增受控：能归并到现有文件的必须归并；只有出现无法归入现有文件的新一级类目时，才允许创建新文档�?- 每个新增或修改的文档、样例、schema、package、data、runtime 文件，都必须能在 `docs/15-系统流程树与扩展问题台账.md` ??`examples/system-process-tree.json` 中找到功能和流程树节点�?- UI 显示类抽象需求的实现前置条件：先检索网络上与该 UI 需求相关的权威或高质量资料，提炼可执行原则，再结合用户目标和当前界面实现；不要仅凭主观直觉调整视觉效果。该规则当前只约??UI 显示部分??
# Verification

- 修改冷启动人物引入、候选人物、角色绑定、场景关系权重、人物确认或关系引用同步时，必须运行 `node --test packages/identity-resolution/tests/*.test.mjs packages/storage-runtime/tests/*.test.mjs`、`npm run identity:cold-start` ??`npm run process-tree:validate`，并同步流程树、Obsidian 视图和文件登记；候选确认或关系变更必须同步 RawEvent/SemanticEvent 的人物与关系引用，并??metadata 中保留旧候选历史�?
- 修改工作流后至少运�??Node 原生测试??- 修改状态笔记后检�?`runtime/state/current-status.json`、`runtime/state/operator-note.md` ??`runtime/state/run-events.jsonl` 是否同步更新�?- 修改状态笔记写入逻辑后必须运�?`node --test packages/agent-runtime/tests/*.test.mjs`，其中包含多进程并发写入测试�?- 正式版完成后需要使用真实运行场景做压力测试，并根据用户专项测试意见迭代??- 图谱相关修改必须验证商业接洽和社会生活两类进程安排，且不得自动发送消息或自动确�??P1 高风险事件�?- 技能接口分为系统可执行能力和人类流程协助能力；任何外部平台交互默�??dry-run，并要求用户确认、权限和审计记录�?- 触发引擎支持用户主动、定时、职业例行、偏好例行和图谱信号触发；第一版只生成计划，不真实调用微信、日历或提醒连接器�??- 触发计划必须保留 `manual_execution_checklist`，用于检查目标对象、草稿、平台预览、确认闸门和行动后反馈�?- 自动化预览第一版只允�??dry-run；本地测试页必须保留 `automation_preview_test_page.v1` 状态契约、本地确认控件和真实发送阻断；平台页面或页面快照必须通�??`platform_dry_run_connector.v1` 检查，证明预览到达、草稿存在、发送被阻断；修�?`AutomationPreview`、`AutomationPreviewTrial`、`PlatformDryRunConnector` 或本地测试页后必须运�?`node --test packages/trigger-engine/tests/*.test.mjs`、`node scripts/run-trigger-demo.mjs` ??`npm run trigger:page`??- 平台测试页或真实测试账号页面快照进入自动化试点前，必须运�?`npm run platform:snapshot:validate` ??`node scripts/validate-platform-snapshot.mjs --snapshot=<platform_snapshot.html> --preview=<automation-preview.json>`；只�?`platform_snapshot_validation.v1.ready_for_platform_dry_run=true`、`send_blocked=true` ??`real_execution_allowed=false` 时，才允许继续扩大自动化预览样本�?- 人物与事件存储实现必须先对�??`docs/12-人物与事件存储方??md`，并保持索引可重建、写入可审计??- 可能性分支由 `packages/possibility-branch` ??`scripts/run-possibility-branch-demo.mjs` 生�??`possibility_branch_analysis.v1`，输出到 `runtime/possibility-branches/**`；它只保存多身份、多事件、嵌套事件和读取权重假设，不写入 `data/people/**`、`data/events/**` ??`data/indexes/**`。修改可能性分�?schema/runtime/script/test、权重公式、读取计划或提升门禁时，必须运行 `node --test packages/possibility-branch/tests/*.test.mjs`、`npm run possibility:branch` ??`npm run process-tree:validate`，并同步 `docs/12`、`docs/15`、`examples/system-process-tree.json`、Obsidian 视图和文件登记�?- 真实聊天、网页或手工记录进入系统前，先按 `schemas/pilot-import-batch.schema.json` 组织�?`PilotImportBatch`，再通�??`npm run pilot:validate` ??`node scripts/validate-pilot-intake.mjs --input=<PilotImportBatch.json>` 生�??`pilot_intake_readiness.v1`；只�?`required_failures` 为空时才允许进入导入�?MVP 试点，完整闭环还需要至�?1 条反馈记录；修改导入或前置门禁逻辑时必须运�?`node --test packages/storage-runtime/tests/*.test.mjs`??- `scripts/validate-pilot-intake.mjs` 会输�?`runtime/intake-validations/<import_id>/pilot-intake-readiness.json` ??Markdown 报告；该产物属于事件记�??/ 审计节点，必须同步登记到流程树�??Obsidian 视图�?- `runMvpLoopFromPilotImport` 会在主闭环内再次执行 `pilot_intake_readiness.v1` 检查；没有达�??`ready_for_closed_loop_mvp` 的样本必须失败退出，不允许继续决策、触发和回写�?- 导入样本进�??MVP 试点时必须优先使??`runMvpLoopFromPilotImport` ??`npm run mvp:import`，确保导入记录能实际驱动决策、触发、反馈、回写、索引和审计；修改该链路时必须运�?`node --test packages/mvp-runtime/tests/*.test.mjs`??- MVP 闭环完成后必须输出完??`real_user_review` ??`optimization_result`，用于从真实用户目标角度检查应用内场景现实逻辑并给出优化；状态摘要中必须保留 `total_goal`、`app_scene`、检查项、反馈摘要、优化焦点，并让 `real_user_review_complete=true`、`optimization_result_complete=true`。修改该逻辑时必须同步流程树�?MVP 测试�?- 应用内报告页�?`writeMvpRunReport` 生成，输出�??`runtime/mvp-reports/**`；修改报告页时必须运�?`node --test packages/mvp-runtime/tests/*.test.mjs`，并同步流程树、Obsidian 视图和文件登记�?- 用户专项测试意见必须�?`schemas/mvp-user-feedback.schema.json` 组织，进??MVP 时输�?`user_test_review` ??`second_pass_optimization`；修改该链路时必须运�?`node --test packages/mvp-runtime/tests/*.test.mjs` 并同步流程树、Obsidian 视图和报告页检查�?- MVP 自代理预检??`runMvpSelfAgentCycle` ??`scripts/run-mvp-self-agent-cycle.mjs` 生成，输出�??`runtime/self-agent-preflights/**`、`runtime/input-kits/**`、`runtime/user-inputs/templates/**`、`runtime/input-templates/**` ??`runtime/input-readiness/**`；它会同时刷新导入闭环、报告页、MVP 完成度审计、流程树同步校验、轻�?MVP 压力测试、外部输入包、安全模板和外部输入就绪报告。修改闭环编排、审计开放项、真实样本、平台快照门禁、流程树同步规则、输入包规则、模板规则、输入就绪规则或压力测试规则时，必须运行 `npm run mvp:self-agent` 并同步流程树、Obsidian 视图和文件登记�?- MVP 外部输入安全模板??`initializeMvpExternalInputTemplates` ??`scripts/init-mvp-external-input-templates.mjs` 生成；正常路径下 `mvp:self-agent` 会自动执行。`npm run mvp:inputs:init` 只用于不重跑完整预检时重生成模板。它只能�?`.template` 参照文件和模板初始化报告，不能写真实目标文件；原�?examples ??`.template` 文件放入真实目标路径必须�?`mvp:inputs:check` 判�??`needs_attention`。修改输入包、模板、就绪校验或用户材料交接规则时，必须运行 `npm run mvp:self-agent`、`npm run mvp:inputs:check`、mvp-runtime 测试，并同步流程树、Obsidian 视图和文件登记�?- PT-003 真实试点样本材料交接包由 `buildPt003PilotMaterials` ??`scripts/prepare-pt003-pilot-materials.mjs` 生成，输出�??`runtime/pt003-pilot-materials/**`；它读取最??readiness 和只读桌??observation，生�?blockers、验收门槛、后续校验命令和 `pilot-import.real.draft.json` 草稿，但不写�?`runtime/user-inputs/pilot-import.real.json`，不触发真实发送。修�?PT-003 材料交接、真实样本草稿、readiness 对齐或用户材料准备规则时，必须运�?`npm run mvp:pt003:prepare`、`npm run mvp:inputs:check`、`npm run mvp:real-trial`、mvp-runtime 测试，并同步流程树、Obsidian 视图和文件登记�?- PT-004 受控平台快照�?`scripts/prepare-controlled-platform-snapshot.mjs` 生成，输??`runtime/user-inputs/platform-snapshot.real.html` ??`runtime/user-inputs/platform-snapshot-preview.real.json`；它只验证预览、草稿和发送阻断，不生�?PT-003 真实试点样本。修改该脚本或平台快照门禁时，必须运�?`npm run mvp:pt004:prepare`、`npm run mvp:inputs:check`、`npm run mvp:real-trial`，并同步流程树、Obsidian 视图和文件登记�?- MVP 真实输入受控试跑??`runMvpRealInputTrial` ??`scripts/run-mvp-real-input-trial.mjs` 生成，输出�??`runtime/real-input-trials/**`；它先复用外部输入就绪校验，PT-003/PT-004 缺失、无效或需修正时必须停止在报告层，只有两�?ready 后才运行完�??MVP 闭环、报告页、完成度审计和流程树同步。`writeMvpRealInputTrial` 必须同时写�??`mvp_real_input_trial_report.v1` HTML 页面，供用户先看应用内试跑状态。修改真实材料交接、外部输??readiness、MVP 导入闭环、完成度审计、试跑报告页或流程树同步时，必须运行 `npm run mvp:real-trial` 的缺材料路径�?ready 路径测试，并同步流程树、Obsidian 视图和文件登记�?- MVP 统一状态看板�??`buildMvpStatusDashboard` ??`scripts/write-mvp-status-dashboard.mjs` 生成，输出�??`runtime/status-dashboards/**`；它汇总自代理、目标审计、只读扩样目标、只读扩样状态、只读重复样本确认、manifest readiness、只读批量采集、统一来源接入矩阵、只读工作包、真实输入试跑、完成度审计、流程树同步、压测和运行状态。运行自代理、真实输入试跑、目标审计、流程树校验、只读扩样状态、只读重复样本确认、只??manifest 校验、统一来源接入矩阵或压测后，应运行 `npm run mvp:status` 刷新用户和外部系统优先查看的当前状态入口�?- 只读批量采�??manifest 交接包由 `buildReadOnlySourceCollectionManifestKit` ??`scripts/init-read-only-source-collection-manifest.mjs` 生成，输出�??`runtime/read-only-source-collection-manifest-kits/**`；它只能写模??manifest 和来源暂存说明，不能写真实目�?manifest 或真实来源文件。manifest 就绪校验??`buildReadOnlySourceCollectionManifestReadiness` ??`scripts/validate-read-only-source-collection-manifest.mjs` 生成，输出�??`runtime/read-only-source-collection-manifest-readiness/**`；它只检查模板标记、占位符、本地保存文件、来源类型和发送阻断，不�??observation。修�?manifest kit、manifest readiness、批量采集入口或未来来源准备规则时，必须运行 `node --test packages/intake-runtime/tests/*.test.mjs` ??`npm run process-tree:validate`，并同步流程树、Obsidian 视图和文件登记�?- 统一来源接入矩阵??`buildSourceIntakeMatrix` ??`scripts/write-source-intake-matrix.mjs` 生成，输出�??`runtime/source-intake-matrix/**`；它只读扫�??WeChat 桌面、网页、外部聊天导出和业�??API 来�??lane ??capability、observation、conformance、RawEvent 映射、生成�??PilotImportBatch 覆盖和真实发送阻断。修改未来来源接入规则、adapter conformance ??observation ??RawEvent 路径时，必须运行 `node --test packages/intake-runtime/tests/*.test.mjs`、`npm run intake:source:matrix` ??`npm run process-tree:validate`??- 只读重复 observation 复核�?`buildReadOnlyDuplicateObservationReview` ??`scripts/review-read-only-duplicate-observations.mjs` 生成，输出�??`runtime/read-only-duplicate-observation-reviews/**`；它只读比较重复组�??observation_id、adapter、platform、摘要、截图大小和发送阻断，给出是否建议去重但仍要求操作者确认。只读重�?observation 操作者确认由 `buildReadOnlyDuplicateObservationConfirmation` ??`scripts/confirm-read-only-duplicate-observations.mjs` 生成，输出�??`runtime/read-only-duplicate-observation-confirmations/**`；未传入 reviewed decision 时只写确认模板，传入后才记录确认结果，且不删除证据、不真实发送。修改去重、样本增长口径、重复复核或确认逻辑时，必须运行 `node --test packages/mvp-runtime/tests/read-only-duplicate-observation-review.test.mjs packages/mvp-runtime/tests/read-only-expansion-status.test.mjs`、`npm run intake:read-only:duplicate:review`、`npm run intake:read-only:duplicate:confirm` ??`npm run process-tree:validate`??- 目标导向图谱工程回测??`runGoalOrientedInteractionBacktest` ??`scripts/run-goal-oriented-backtest.mjs` 生成，输出�??`runtime/goal-oriented-backtests/**`，并刷新 `tupu/07-理论与代码工程回测一致性结�?md`；它读取 `tupu/05` 的理论逐轮回测，要求三类场景十二轮、理论上下文、代码上下文、策略演化、具体草稿和回写计划全部完整，且 `theory_code_match_rate=1`、`hard_exit_signals=[]`。修改理论回测、工程回�?schema/runtime/script/test ??`tupu` 对齐文档时，必须运行 `node --test packages/mvp-runtime/tests/*.test.mjs`、`npm run tupu:backtest` ??`npm run process-tree:validate`??- 工具调用模块??`packages/tool-runtime` ??`scripts/run-tool-runtime-demo.mjs` 生�??`ToolAdapterCapability`、`social_tool_call_plan.v1` ??`social_tool_call_result.v1`；它默认只�??dry-run，不执行外部命令，不真实发送消息。修�?CLI-Anything 接入、工具能力分级、调用计划、dry-run 结果或外部协助软件闸门时，必须运�?`node --test packages/tool-runtime/tests/*.test.mjs`、`npm run tool:demo` ??`npm run process-tree:validate`，并同步流程树、Obsidian 视图和文件登记�?- PT-028 最终反馈决策包�?`scripts/write-pt028-final-feedback-decision-pack.mjs` 生�??`pt028_final_feedback_decision_pack.v1`，输出到 `runtime/pt028-final-feedback-decision-packs/**`；它刷新 workpack、确认模板和验收链，把真实多窗口反馈缺口、窗口级反馈核对�?`pt028_operator_feedback_window_checklist.v1`、可填写确认模板、下一步命令、HTML 操作者入口�??no-send 边界汇成一个操作者入口，默认不写真实反馈目标文件、不真实发送。PT-028 最终反馈交接包校验??`scripts/validate-pt028-feedback-handoff.mjs` 生�??`pt028_feedback_handoff_validation.v1`，输出到 `runtime/pt028-feedback-handoff-validations/**`；它只读检??HTML、窗口核对表、确认模板、预检、验收链??no-send 边界是否齐全。PT-028 逐窗口反馈采集会话�??`scripts/write-pt028-feedback-collection-session.mjs` 生�??`pt028_feedback_collection_session.v1`，输出到 `runtime/pt028-feedback-collection-sessions/**`；它把已通过交接校验的窗口核对表转成操作者逐窗口采集任务，记录确认模板指针、证据引用、prompt-only/no-send 边界和下一步预检命令，仍不写真实反馈目标文件。`scripts/write-pt028-gui-event-stream.mjs` 支持 `--session=<pt028_feedback_collection_session.v1>`，可在真实反馈未完成前把采集会话的多窗�??state_path 预绑定到低延迟事件流，但不构成最终生产验收。PT-028 采集覆盖校验�?`scripts/validate-pt028-feedback-collection-coverage.mjs` 生�??`pt028_feedback_collection_coverage.v1`，输出到 `runtime/pt028-feedback-collection-coverages/**`；它只读比对采集会话任务和人工填写后的确认模板，证明每个窗口任务都有匹配记录和人工确认后才进入确认预检。PT-028 真实反馈确认预检??`scripts/preflight-pt028-real-feedback-confirmation.mjs` 生�??`pt028_feedback_confirmation_preflight.v1`，输出到 `runtime/pt028-feedback-confirmation-preflights/**`；它复用确认门、coverage ??readiness 规则，只诊断确认模板缺口，永不写真实反馈目标文件。PT-028 最终专项验收链??`scripts/run-pt028-acceptance-chain.mjs` 生�??`pt028_acceptance_chain.v1`，输出到 `runtime/pt028-acceptance-chains/**`；它串联受控确认、feedback-bound 低延迟事件流、真实反馈就绪校验、多窗口反馈校准、PT-028 审计和最终专项验收，默认不真实发送；无真??feedback 时会优先使用最新采集会话生成多窗口低延迟事件流预绑定证据。修改确认门、确认预检、最终反馈决策包、交接包校验、反馈采集会话、采集覆盖校验、验收链、最终验收显式证据输入或链路报告时，必须运行 `node --test packages/decision-cluster/tests/decision-cluster.test.mjs`、`npm run pt028:feedback-decision-pack`、`npm run pt028:feedback-handoff:validate`、`npm run pt028:feedback-collection:session`、`npm run pt028:event-stream -- --session=runtime/pt028-feedback-collection-sessions/latest.json`、`npm run pt028:feedback-collection:coverage`、`npm run pt028:feedback-confirm:preflight`、`npm run pt028:acceptance-chain` ??`npm run process-tree:validate`??- MVP 完成度审计由 `auditMvpCompletionEvidence` ??`scripts/audit-mvp-completion.mjs` 生成，输出�??`runtime/audits/**`；修改闭环证据、报告页契约、流程树问题台账或状态笔记时，必须运�?`npm run mvp:audit` 并检�?required failures 是否为空??- MVP 总目标逐项审计??`auditMvpObjectiveEvidence` ??`scripts/audit-mvp-objective.mjs` 生成，输出�??`runtime/objective-audits/**`；它读取自代理预检、MVP 完成度审计和真实输入试跑证据，区分本地目标证据完整性、PT-003/PT-004 真实外部输入阻断项、真实输入试跑门槛和问题台账复核门槛。修改总目标、自代理预检、输入包、模板、就绪报告、真实输入试跑或完成度审计时，必须运�?`npm run mvp:objective:audit` 并检�?required failures 是否为空??- 最终验证时不要并行运�??`npm run mvp:self-agent` ??`npm run mvp:audit`；先等自代理完成，再运�??MVP 审计，否则审计可能读到中间运行态并触�??`state_last_run_completed`??- 流程树同步校验�??`validateProcessTreeSync` ??`scripts/validate-process-tree.mjs` 生成，输出�??`runtime/process-tree-validations/**`；修改流程树结构、节点状态、问题台账、文件功能登记或 Obsidian 视图时，必须运行 `npm run process-tree:validate` 并检�?required failures 是否为空??- MVP 全链路压力测试由 `runMvpStressTest` ??`scripts/stress-mvp-loop.mjs` 生成，输出�??`runtime/mvp-stress-tests/**`；修�?MVP 导入闭环、真实用户目标回顾、专项反馈、报告渲染、流程树校验或停止标准时，必须运�?`npm run stress:mvp` 或更小的 mvp-runtime 压力测试，并检�?hard_exit_signals 为空�?- 执行 `docs/10-第一阶段执行方�??md` 前，必须检�?`docs/13-文档互证审计与修复清??md` 是否存在未关闭阻断项�?- 如果文档、schema 和代码之间出现冲突，先暂停实现，记录冲突点，更新权威文档后再继续??- `packages/storage-runtime` 负责本�??`data/` 写入、审计和索引重建；不要把业务存储写进 `runtime/state/`??- `packages/mvp-runtime` 负责串联状态、存储、决策、触发、反馈、回写和索引，并支持批量试点门禁汇总；修�??MVP 闭环时必须同�?`scripts/run-mvp-loop.mjs`、流程树和测试�?- `examples/closed-loop-storage-mapping.json` ??`docs/10` 三条闭环样例�?`docs/12` 存储对象的基准映射，修改闭环流程时必须同步测试�??- `docs/15-系统流程树与扩展问题台账.md` 是当前系统架构入口；新发现的问题必须挂到流程树节点，不要只留在对话中??- `examples/system-process-tree.json` 是机器可读流程树；修改主流程、节点、问题台账时必须同步它�??- `views/obsidian/system-process-tree.md` ??Obsidian Markdown 显化流程树；修改主流程、节点、问题台账或流程树结构时必须同步它�??- `views/obsidian/system-process-tree.canvas` ??Obsidian 可视化流程树；修改主流程、节点、问题台账或流程树结构时必须同步它�??- 新需求添加后必须执行文档同步检查：是否影�??README、AGENTS、docs/04、docs/10、docs/11、docs/12、docs/13、docs/15、examples/system-process-tree.json、views/obsidian/system-process-tree.md、views/obsidian/system-process-tree.canvas、闭环样例或测试??- 执行文档同步检查后，�??`npm run process-tree:validate` 生成外部可读证据；如果失败，先修复登记或 Obsidian 视图再继续实现�?
# Notes

- 原始方案文件保留在根目录：`产品方�??md`、`能力构建方�??,md`??- 权威可维护方案放??`docs/`??- 运行状态笔记是项目一等能力，必须能被外部系统直接读取和检查�?- `docs/12-人物与事件存储方??md` 是后续人物存储和事件存储骨架的执行依据�??- `docs/13-文档互证审计与修复清??md` 是跨文档一致性和脱节检查入口�?- `docs/14-闭环样例映射与流程一致性审�?md` 是闭环样例和初始目标一致性审查入口�??- `docs/15-系统流程树与扩展问题台账.md` 是流程树、扩展点和问题台账入口�?- `views/obsidian/system-process-tree.md` 是流程树??Obsidian Markdown 观察入口??- `views/obsidian/system-process-tree.canvas` 是流程树??Obsidian 显化观察入口�?- `examples/pilot-import-batch.sample.json` 是真实样本导入格式的最小可运行样例??- `examples/mvp-user-feedback.sample.json` 是用户专项测试反馈的最小可运行样例??- `runtime/input-kits/**` 是自代理生成的真实材料交接入口，包�??PT-003/PT-004 的目标文件、必备内容、校验命令和验收门槛�?- `runtime/user-inputs/templates/**` ??`npm run mvp:inputs:init` 生成的安全模板目录，只作为用户准备真实材料的参照，不作为真实输入读取??- `runtime/input-templates/**` ??`npm run mvp:inputs:init` 生成的模板初始化报告，记录来源输入包、模板路径、故意不写入的真实目标文件和后续命令�?- `runtime/input-readiness/**` 是自代理??`npm run mvp:inputs:check` 生成的真实材料就绪报告，聚合判断 PT-003/PT-004 是否缺失、无效、需修正或可进入下一步�?- `runtime/pt003-pilot-materials/**` ??`npm run mvp:pt003:prepare` 生成�?PT-003 真实样本材料交接包，说明哪些只读证据可作为附件、哪些真实文�?身�??关�??反馈仍需用户补齐，并提供不写真实目标文件的草稿�?- `runtime/objective-audits/**` ??`npm run mvp:objective:audit` 生成的总目标逐项审计，说明当前目标已证明到哪一步、是否仍等待真实外部输入??- `runtime/real-input-trials/**` ??`npm run mvp:real-trial` 生成的真实输入试跑证据，说�??PT-003/PT-004 是�??ready、是否已跑完整闭环，以及是否可进入问题台账复核；其�??`mvp-real-input-trial-report.html` 是优先给人看的应用内视图??- `runtime/status-dashboards/**` ??`npm run mvp:status` 生成的统一 MVP 状态入口，说明当前完成内容、只读扩样状态、来源接入矩阵、阻断项、下一步和关键产物路径�?- `runtime/read-only-source-collection-manifest-kits/**` ??`npm run intake:read-only:manifest:init` 生成的只读批量采??manifest 交接入口，只包含模板和说明，不作为真实样本或真�??manifest 完成证据??- `runtime/read-only-source-collection-manifest-readiness/**` ??`npm run intake:read-only:manifest:check` 生成的只读批量采??manifest 就绪报告，只�?`ready_for_collection=true` ??required failures 为空时才进�??collect??- `runtime/user-inputs/read-only-sources/**` 是只读批量采集来源暂存目录；生成�?README 或模板说明不作为真实样本完成证据??- `runtime/source-intake-matrix/**` ??`npm run intake:source:matrix` 生成的统一来源接入矩阵，说明每条来�?lane 是否具备 conformance、真实只�?observation、RawEvent 映射、生成�??PilotImportBatch 覆盖和真实发送阻断�?- `runtime/read-only-duplicate-observation-reviews/**` ??`npm run intake:read-only:duplicate:review` 生成的只读重�?observation 复核报告，说明重复组是否具备确定性去重证据、是否仍需操作者确认，以及真实发送是否继续阻断�?- `runtime/read-only-duplicate-observation-confirmations/**` ??`npm run intake:read-only:duplicate:confirm` 生成的只读重�?observation 操作者确认报告�??decision 模板；只??reviewed decision 覆盖当前重复组时，状态层才会关闭重复复核 warning??- `runtime/goal-oriented-backtests/**` ??`npm run tupu:backtest` 生成的目标导向图谱工程回测证据，说明理论逐轮回测与代码侧上下文、策略、草稿和回写是否一致�??- `runtime/possibility-branches/**` ??`npm run possibility:branch` 或可能性分支运行时生成的独立假设产物，说明复杂输入中的目标人物多重身份、多事件、嵌套事件、权重匹配、读取计划和提升门禁；它不是人物/事件主图谱事实保存区�?- `packages/tool-runtime` 是社交辅助系统的工具调用模块，用于接??CLI-Anything 和未来外部协助软件；第一版只生成能力登记、调用计划�??dry-run 回执�?
# Skill Creation Default Gate

- Before creating any new Skill, plugin-facing workflow, or mandatory capability guide, first perform a reuse search. Check the currently available Skills, installed plugins/connectors, local repository modules, `capability-upgrade-registry`, CLI-Anything or other known local tool catalogs, and reputable external software or open-source projects when network research is appropriate.
- A new Skill is allowed only after the reuse search shows that existing Skills/software cannot satisfy the goal, cannot be safely adapted, or would cost more to adapt than a small purpose-built Skill. Record the decision in a `skill_creation_gate.v1` artifact or an equivalent task note before creating files.
- The default decision order is: reuse existing Skill/tool -> wrap or adapt existing capability -> install/request an existing plugin/connector when explicitly requested and available -> create a new Skill only as the last step.
- The gate record must include the requested goal, searched sources, candidates found, why each candidate was accepted or rejected, license/security notes when external software is considered, and the final create/reuse decision.
- Do not create a new Skill just because it is convenient. Treat Skill creation as a system capability change that needs evidence, validation, and a clear future trigger condition.

# System Issue Debugging Rule

- 当用户提出系统问题、异常、故障、行为不符合预期或需要修复的请求时，必须先完成问题理解、证据收集、复现测试或环境验证，再进行代码修改。
- 禁止在没有检查现象、日志、接口、运行环境和相关边界前盲猜式修改。
- 修复流程必须遵循：复述问题理解 -> 定位相关模块和上下游接口 -> 读取当前实现 -> 设计最小验证方式 -> 复现或证伪问题 -> 执行最小必要修改 -> 运行回归验证 -> 向用户说明原因、改动和验证结果。
- 如果问题无法在本地复现，必须明确说明已验证的内容、未能验证的内容和下一步需要的用户侧证据，不能把推测当作结论。
- 对 UI、悬浮窗、语音输入输出、3D 粒子 OS、状态对话框等正在并行开发的模块，修改前必须确认本次改动边界，避免影响其他线程和既有功能。
