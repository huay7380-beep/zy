# Social Graph

`social-graph` 提供人物关系、事件线索和进程安排的第一版规则运行时。

它不负责自动采集外部数据，也不自动写入高风险事件。第一版只处理用户显式提供的图谱快照和上下文。

## 能力

- 根据 `primary_person_id` 查找关系边。
- 从关系类型库加载行为准则。
- 从历史事件中提取近期线索。
- 从当前上下文提取候选事件。
- 根据用户初始目标生成后续进程安排。
